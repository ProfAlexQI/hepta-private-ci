name: hepta-vnext self-hosted exact qualification

on:
  workflow_dispatch:
  push:
    branches:
      - qualification/selfhosted-52ec

permissions:
  contents: read

env:
  HEPTA_EXPECTED_HEAD: 52ec4b3868fc5272e19ed516d00e11e44c549ea4
  HEPTA_EXPECTED_TREE: 247e9e7cfcb41dbfcc8c5b3b531b1e1407c0bd5d

jobs:
  portable-product-linux:
    name: Product (ubuntu-24.04)
    runs-on: hepta-vnext-linux-x64-52ec
    timeout-minutes: 90
    defaults:
      run:
        working-directory: codex-rs
    steps:
      - name: Checkout exact candidate 52ec
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: 52ec4b3868fc5272e19ed516d00e11e44c549ea4
          persist-credentials: false

      - name: Verify exact candidate source preflight
        shell: bash
        working-directory: .
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$HEPTA_EXPECTED_HEAD"
          test "$(git rev-parse 'HEAD^{tree}')" = "$HEPTA_EXPECTED_TREE"
          test -z "$(git status --porcelain=v1 --untracked-files=all)"

      - uses: ./.github/actions/setup-ci

      - name: Install Linux build dependencies
        if: runner.os == 'Linux'
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update -y
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends pkg-config libcap-dev bubblewrap

      - uses: dtolnay/rust-toolchain@e081816240890017053eacbb1bdf337761dc5582 # 1.95.0
        with:
          components: rustfmt, clippy

      - name: Locked workspace metadata
        shell: bash
        run: cargo metadata --locked --no-deps --format-version 1 >/dev/null

      - name: Portable Hepta tests
        shell: bash
        run: |
          set -euo pipefail
          cargo test --locked \
            -p codex-hepta-contracts \
            -p codex-hepta-evidence \
            -p codex-hepta-governance \
            -p codex-hepta-memory \
            -p codex-hepta-memory-extension \
            -p codex-hepta-paths \
            -p codex-hepta-runtime \
            -p codex-hepta-native-gateway

      - name: Product caller compilation
        shell: bash
        run: |
          set -euo pipefail
          cargo check --locked \
            -p codex-core \
            -p codex-app-server \
            -p codex-mcp-server \
            -p codex-cli

      - name: Strict Hepta Clippy
        shell: bash
        run: |
          set -euo pipefail
          cargo clippy --locked --all-targets \
            -p codex-hepta-contracts \
            -p codex-hepta-evidence \
            -p codex-hepta-governance \
            -p codex-hepta-memory \
            -p codex-hepta-memory-extension \
            -p codex-hepta-paths \
            -p codex-hepta-runtime \
            -p codex-hepta-native-gateway \
            -- -D warnings

      - name: Verify exact candidate source postflight
        if: always() && !cancelled()
        shell: bash
        working-directory: .
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$HEPTA_EXPECTED_HEAD"
          test "$(git rev-parse 'HEAD^{tree}')" = "$HEPTA_EXPECTED_TREE"
          test -z "$(git status --porcelain=v1 --untracked-files=all)"

  portable-product-windows:
    name: Product (windows-latest)
    runs-on: hepta-vnext-windows-x64-52ec
    timeout-minutes: 90
    defaults:
      run:
        working-directory: codex-rs
    steps:
      - name: Checkout exact candidate 52ec
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: 52ec4b3868fc5272e19ed516d00e11e44c549ea4
          persist-credentials: false

      - name: Verify exact candidate source preflight
        shell: bash
        working-directory: .
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$HEPTA_EXPECTED_HEAD"
          test "$(git rev-parse 'HEAD^{tree}')" = "$HEPTA_EXPECTED_TREE"
          test -z "$(git status --porcelain=v1 --untracked-files=all)"

      - uses: ./.github/actions/setup-ci

      - name: Install Linux build dependencies
        if: runner.os == 'Linux'
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update -y
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends pkg-config libcap-dev bubblewrap

      - uses: dtolnay/rust-toolchain@e081816240890017053eacbb1bdf337761dc5582 # 1.95.0
        with:
          components: rustfmt, clippy

      - name: Locked workspace metadata
        shell: bash
        run: cargo metadata --locked --no-deps --format-version 1 >/dev/null

      - name: Portable Hepta tests
        shell: bash
        run: |
          set -euo pipefail
          cargo test --locked \
            -p codex-hepta-contracts \
            -p codex-hepta-evidence \
            -p codex-hepta-governance \
            -p codex-hepta-memory \
            -p codex-hepta-memory-extension \
            -p codex-hepta-paths \
            -p codex-hepta-runtime \
            -p codex-hepta-native-gateway

      - name: Product caller compilation
        shell: bash
        run: |
          set -euo pipefail
          cargo check --locked \
            -p codex-core \
            -p codex-app-server \
            -p codex-mcp-server \
            -p codex-cli

      - name: Strict Hepta Clippy
        shell: bash
        run: |
          set -euo pipefail
          cargo clippy --locked --all-targets \
            -p codex-hepta-contracts \
            -p codex-hepta-evidence \
            -p codex-hepta-governance \
            -p codex-hepta-memory \
            -p codex-hepta-memory-extension \
            -p codex-hepta-paths \
            -p codex-hepta-runtime \
            -p codex-hepta-native-gateway \
            -- -D warnings

      - name: Verify exact candidate source postflight
        if: always() && !cancelled()
        shell: bash
        working-directory: .
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$HEPTA_EXPECTED_HEAD"
          test "$(git rev-parse 'HEAD^{tree}')" = "$HEPTA_EXPECTED_TREE"
          test -z "$(git status --porcelain=v1 --untracked-files=all)"

  generated-and-locks:
    name: Generated artifacts and locks
    runs-on: hepta-vnext-linux-x64-52ec
    timeout-minutes: 60
    steps:
      - name: Checkout exact candidate 52ec
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: 52ec4b3868fc5272e19ed516d00e11e44c549ea4
          persist-credentials: false

      - name: Verify exact candidate source preflight
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$HEPTA_EXPECTED_HEAD"
          test "$(git rev-parse 'HEAD^{tree}')" = "$HEPTA_EXPECTED_TREE"
          test -z "$(git status --porcelain=v1 --untracked-files=all)"

      - uses: ./.github/actions/setup-ci

      - uses: dtolnay/rust-toolchain@e081816240890017053eacbb1bdf337761dc5582 # 1.95.0
        with:
          components: rustfmt

      - name: Format
        working-directory: codex-rs
        run: cargo fmt --all -- --check

      - name: App Server schemas are current
        run: |
          set -euo pipefail
          just write-app-server-schema --experimental
          git diff --exit-code

      - name: Bazel lock is current
        run: just bazel-lock-check

      - name: Clean worktree
        if: always() && !cancelled()
        uses: ./.github/actions/check-clean-worktree

      - name: Verify exact candidate source postflight
        if: always() && !cancelled()
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$HEPTA_EXPECTED_HEAD"
          test "$(git rev-parse 'HEAD^{tree}')" = "$HEPTA_EXPECTED_TREE"
          test -z "$(git status --porcelain=v1 --untracked-files=all)"
