name: P0.3.4 strict paired gate invalidated pending structural closure

on:
  push:
    branches:
      - codex/p034-rebuild-v2-20260829
    paths:
      - .github/workflows/p034-strict-paired.yml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: p034-strict-paired
  cancel-in-progress: true

jobs:
  invalidate:
    name: Invalidate suppression-based candidate gate
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Fail closed without publication
        shell: bash
        run: |
          set -euo pipefail
          echo "The previous item-level lint-suppression design is invalid under Plan v3.15."
          echo "No candidate branch, status, pull request, or authority state is changed."
