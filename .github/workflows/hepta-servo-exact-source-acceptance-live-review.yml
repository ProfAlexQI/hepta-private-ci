name: hepta Servo exact source acceptance live review legacy wrapper

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  trusted-base-v2:
    name: Delegate to trusted-base source acceptance live review v2
    uses: ./.github/workflows/hepta-servo-exact-source-acceptance-live-review-v2.yml
    secrets: inherit
