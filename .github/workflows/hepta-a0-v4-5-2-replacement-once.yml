name: Retired Hepta A0 V4.5.2 source publisher

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Fail closed
        shell: bash
        run: |
          echo 'RETIRED_HEPTA_A0_V4_5_2_SOURCE_PUBLISHER'
          echo 'The governed payload was revoked after the publisher remained runner-zero.'
          exit 1
