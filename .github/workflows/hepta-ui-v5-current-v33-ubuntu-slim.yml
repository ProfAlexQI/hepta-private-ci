name: Hepta UI v5 current v33 Ubuntu slim superseded

on:
  push:
    branches:
      - ops/ui-v5-current-v33-ubuntu-slim-20260830
    paths:
      - .github/workflows/hepta-ui-v5-current-v33-ubuntu-slim.yml

permissions:
  contents: read

concurrency:
  group: hepta-ui-v5-current-v33-ubuntu-slim
  cancel-in-progress: true

jobs:
  superseded:
    if: ${{ false }}
    runs-on: ubuntu-slim
    steps:
      - run: echo "superseded after v31 acquired an assigned runner"
