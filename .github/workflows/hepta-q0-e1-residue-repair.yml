name: Hepta Q0 E1 blocker closure

on:
  push:
    branches:
      - codex/hepta-intelligence-plan-v3-20260828
    paths:
      - .github/workflows/hepta-q0-e1-residue-repair.yml

permissions:
  contents: write

concurrency:
  group: hepta-q0-e1-residue-repair
  cancel-in-progress: false

jobs:
  repair:
    name: Close every remaining Q0 repository blocker
    runs-on: ubuntu-24.04
    timeout-minutes: 360
    steps:
      - name: Checkout exact repair carrier
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: ${{ github.sha }}
          fetch-depth: 2
          persist-credentials: true

      - name: Assert exact carrier identity
        shell: bash
        env:
          EXPECTED_REPOSITORY: ProfHepta/hepta-private-ci
          EXPECTED_BRANCH: codex/hepta-intelligence-plan-v3-20260828
          EXPECTED_PARENT: cf71c9b0316992d07fdad630b7965305c92c4ca1
        run: |
          set -euo pipefail
          test "$GITHUB_REPOSITORY" = "$EXPECTED_REPOSITORY"
          test "$GITHUB_REF_NAME" = "$EXPECTED_BRANCH"
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          test "$(git rev-parse HEAD^)" = "$EXPECTED_PARENT"

      - name: Install repository Rust toolchain
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: 1.95.0
          components: rustfmt,clippy

      - name: Apply exact qualification-only source repairs
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/q0-qualification/close-final-gaps.py
          (cd codex-rs && cargo fmt --all)
          (cd codex-rs && cargo fmt --all -- --check)
          git diff --check

      - name: Apply machine-applicable canonical Clippy repairs
        shell: bash
        run: |
          set -euo pipefail
          run_fix() {
            local label="$1"
            shift
            echo "::group::clippy-fix ${label}"
            set +e
            (cd codex-rs && cargo clippy --fix --locked --allow-dirty --allow-staged "$@")
            local code=$?
            set -e
            echo "clippy_fix_${label//-/_}_exit=${code}"
            echo "::endgroup::"
          }

          run_fix memory \
            -p codex-hepta-memory --all-targets --no-deps -- -D warnings
          run_fix extension \
            -p codex-hepta-memory-extension --all-targets --no-deps -- -D warnings
          run_fix agentd-default \
            -p codex-hepta-agentd --all-targets --no-deps -- -D warnings
          run_fix agentd-shadow \
            -p codex-hepta-agentd \
            --features qualification-intelligence-mutation-shadow \
            --all-targets --no-deps -- -D warnings

          (cd codex-rs && cargo fmt --all)
          (cd codex-rs && cargo fmt --all -- --check)
          git diff --check

      - name: Require every canonical strict Clippy profile to pass
        shell: bash
        run: |
          set -euo pipefail
          output_dir="$RUNNER_TEMP/hepta-q0-strict-clippy"
          rm -rf "$output_dir"
          mkdir -p "$output_dir"
          failed=0

          run_strict() {
            local label="$1"
            shift
            echo "::group::strict-clippy ${label}"
            set +e
            (cd codex-rs && cargo clippy --locked "$@") \
              2>&1 | tee "$output_dir/${label}.log"
            local code=${PIPESTATUS[0]}
            set -e
            printf '%s\n' "$code" > "$output_dir/${label}.exit"
            echo "strict_clippy_${label//-/_}_exit=${code}"
            echo "::endgroup::"
            if [[ "$code" != 0 ]]; then
              failed=1
            fi
          }

          run_strict memory \
            -p codex-hepta-memory --all-targets --no-deps -- -D warnings
          run_strict agent-protocol \
            -p codex-hepta-agent-protocol --all-targets --no-deps -- -D warnings
          run_strict supervisor \
            -p codex-hepta-supervisor --all-targets --no-deps -- -D warnings
          run_strict extension \
            -p codex-hepta-memory-extension --all-targets --no-deps -- -D warnings
          run_strict agentd-default \
            -p codex-hepta-agentd --all-targets --no-deps -- -D warnings
          run_strict agentd-shadow \
            -p codex-hepta-agentd \
            --features qualification-intelligence-mutation-shadow \
            --all-targets --no-deps -- -D warnings

          test "$failed" = 0
          (cd codex-rs && cargo fmt --all -- --check)
          git diff --check

      - name: Verify all source gates and the complete Q0 Rust matrix
        shell: bash
        run: |
          set -euo pipefail
          carrier=.github/workflows/hepta-q0-e1-residue-repair.yml
          payload=scripts/q0-qualification/close-final-gaps.py
          rm "$carrier" "$payload"

          artifact_dir="$RUNNER_TEMP/hepta-q0-final-gap-closure"
          rm -rf "$artifact_dir"
          mkdir -p "$artifact_dir/gates" "$artifact_dir/rust"

          ARTIFACT_DIR="$artifact_dir" \
            bash scripts/q0-qualification/10-source-gates.sh
          ARTIFACT_DIR="$artifact_dir" \
            bash scripts/q0-qualification/20-rust-matrix.sh

          python3 - "$artifact_dir" <<'PY'
          from pathlib import Path
          import sys

          root = Path(sys.argv[1])
          exits = sorted(root.glob("*/*.exit"))
          if not exits:
              raise AssertionError("qualification emitted no exit records")
          failed = {
              str(path.relative_to(root)): path.read_text(encoding="utf-8").strip()
              for path in exits
              if path.read_text(encoding="utf-8").strip() != "0"
          }
          if failed:
              raise AssertionError(f"qualification failures: {failed}")
          print(f"verified_exit_count={len(exits)}")
          PY

          rm -rf scripts/__pycache__ .github/scripts/__pycache__
          (cd codex-rs && cargo fmt --all -- --check)
          git diff --check

      - name: Commit verified repair, bind final candidate, and retire carrier
        shell: bash
        run: |
          set -euo pipefail
          rm -rf scripts/__pycache__ .github/scripts/__pycache__

          python3 - <<'PY'
          import subprocess

          changed = subprocess.check_output(
              ["git", "diff", "--name-only"], text=True
          ).splitlines()
          required = {
              ".github/workflows/hepta-q0-e1-residue-repair.yml",
              "scripts/q0-qualification/close-final-gaps.py",
              "codex-rs/hepta-agentd/src/app_runtime.rs",
              "codex-rs/hepta-memory/src/fact_grounding/durable/grounding/ledger.rs",
              "codex-rs/hepta-memory/src/fact_grounding/durable/grounding/ledger/insert.rs",
              "codex-rs/hepta-memory/src/fact_grounding/durable/grounding/ledger/support.rs",
              "codex-rs/hepta-memory/src/fact_grounding/durable/grounding/ledger/verify.rs",
              "codex-rs/hepta-memory/src/fact_grounding/durable/grounding/prepare.rs",
              "codex-rs/hepta-memory/src/fact_grounding/durable/schema.rs",
              "codex-rs/hepta-memory/src/intelligence_mutation_shadow_host.rs",
              "codex-rs/hepta-memory/src/intelligence_mutation_state.rs",
              "scripts/verify-hepta-intelligence-mutation-journal.py",
              "scripts/verify-hepta-intelligence-mutation-state.py",
          }
          missing = sorted(required.difference(changed))
          if missing:
              raise AssertionError(f"required repair paths missing: {missing}")

          exact_allowed = {
              ".github/workflows/hepta-q0-e1-residue-repair.yml",
              "scripts/q0-qualification/close-final-gaps.py",
              "scripts/verify-hepta-intelligence-mutation-journal.py",
              "scripts/verify-hepta-intelligence-mutation-state.py",
          }
          allowed_prefixes = (
              "codex-rs/hepta-memory/",
              "codex-rs/ext/hepta-memory/",
              "codex-rs/hepta-agentd/",
          )
          unexpected = sorted(
              path
              for path in changed
              if path not in exact_allowed
              and not path.startswith(allowed_prefixes)
          )
          if unexpected:
              raise AssertionError(f"unexpected repair paths: {unexpected}")
          print(f"verified_repair_path_count={len(changed)}")
          PY

          remote_head="$(
            git ls-remote origin "refs/heads/${GITHUB_REF_NAME}" | cut -f1
          )"
          test "$remote_head" = "$GITHUB_SHA"

          git config user.name "hepta-q0-bot"
          git config user.email "hepta-q0-bot@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "fix(intelligence): close remaining Q0 repository blockers"
          repair_sha="$(git rev-parse HEAD)"

          REPAIR_SHA="$repair_sha" python3 - <<'PY'
          import os
          from pathlib import Path

          path = Path(
              ".github/workflows/hepta-intelligence-q0-paired-candidate-v10.yml"
          )
          text = path.read_text(encoding="utf-8")
          old = "  EXPECTED_PARENT: b445bfbfd22efb5ba81ed66b1b68b27934ccc701\n"
          new = f"  EXPECTED_PARENT: {os.environ['REPAIR_SHA']}\n"
          if text.count(old) != 1:
              raise AssertionError("paired workflow parent binding drifted")
          path.write_text(text.replace(old, new, 1), encoding="utf-8")
          PY

          test "$(git diff --name-only | wc -l)" = "1"
          test "$(git diff --name-only)" = \
            ".github/workflows/hepta-intelligence-q0-paired-candidate-v10.yml"
          git diff --check
          python3 scripts/verify-hepta-intelligence-q0-workflow-consolidation.py
          git add .github/workflows/hepta-intelligence-q0-paired-candidate-v10.yml
          git diff --cached --check
          git commit -m "ci(q0): bind candidate to final blocker closure"

          test "$(git rev-parse HEAD^)" = "$repair_sha"
          remote_head="$(
            git ls-remote origin "refs/heads/${GITHUB_REF_NAME}" | cut -f1
          )"
          test "$remote_head" = "$GITHUB_SHA"
          git push origin "HEAD:${GITHUB_REF_NAME}"
