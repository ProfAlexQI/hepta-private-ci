name: Temporary exact Prettier materialization for PR 295 r4

on:
  push:
    branches:
      - admin/format-pr295-bee6217-20260902
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: temporary-format-pr295-r4
  cancel-in-progress: true

jobs:
  format:
    runs-on: macos-15
    timeout-minutes: 20
    steps:
      - name: Check out exact immutable candidate
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: 0c46a7a6ff662a710b6fc7beb0212a9c3e365d19
          fetch-depth: 1
          persist-credentials: false

      - name: Set up pnpm
        uses: pnpm/action-setup@a8198c4bff370c8506180b035930dea56dbd5288 # v5
        with:
          run_install: false

      - name: Set up Node.js
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          node-version: 22
          cache: pnpm

      - name: Format bounded paths and retain exact bytes
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = 0c46a7a6ff662a710b6fc7beb0212a9c3e365d19
          pnpm install --frozen-lockfile
          pnpm exec prettier --write \
            docs/STATUS.md \
            .github/workflows/hepta-algorithm-docs.yml \
            .github/workflows/hepta-cns-embodiment.yml \
            .github/workflows/hepta-repository-integrity.yml
          pnpm exec prettier --check \
            docs/STATUS.md \
            .github/workflows/hepta-algorithm-docs.yml \
            .github/workflows/hepta-cns-embodiment.yml \
            .github/workflows/hepta-repository-integrity.yml
          cat > "$RUNNER_TEMP/expected" <<'EOF'
          .github/workflows/hepta-algorithm-docs.yml
          .github/workflows/hepta-cns-embodiment.yml
          .github/workflows/hepta-repository-integrity.yml
          docs/STATUS.md
          EOF
          git diff --name-only | sort > "$RUNNER_TEMP/actual"
          sort -o "$RUNNER_TEMP/expected" "$RUNNER_TEMP/expected"
          diff -u "$RUNNER_TEMP/expected" "$RUNNER_TEMP/actual"
          git diff --check
          mkdir -p "$RUNNER_TEMP/pr295-r4-prettier/.github/workflows" "$RUNNER_TEMP/pr295-r4-prettier/docs"
          cp .github/workflows/hepta-algorithm-docs.yml "$RUNNER_TEMP/pr295-r4-prettier/.github/workflows/"
          cp .github/workflows/hepta-cns-embodiment.yml "$RUNNER_TEMP/pr295-r4-prettier/.github/workflows/"
          cp .github/workflows/hepta-repository-integrity.yml "$RUNNER_TEMP/pr295-r4-prettier/.github/workflows/"
          cp docs/STATUS.md "$RUNNER_TEMP/pr295-r4-prettier/docs/"
          git diff --binary > "$RUNNER_TEMP/pr295-r4-prettier/prettier.patch"
          shasum -a 256 \
            "$RUNNER_TEMP/pr295-r4-prettier/.github/workflows/hepta-algorithm-docs.yml" \
            "$RUNNER_TEMP/pr295-r4-prettier/.github/workflows/hepta-cns-embodiment.yml" \
            "$RUNNER_TEMP/pr295-r4-prettier/.github/workflows/hepta-repository-integrity.yml" \
            "$RUNNER_TEMP/pr295-r4-prettier/docs/STATUS.md" \
            > "$RUNNER_TEMP/pr295-r4-prettier/SHA256SUMS"

      - name: Upload bounded Prettier output
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: pr295-r4-prettier-0c46a7a
          path: ${{ runner.temp }}/pr295-r4-prettier
          if-no-files-found: error
          retention-days: 1
