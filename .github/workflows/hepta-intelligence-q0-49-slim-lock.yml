name: Hepta Intelligence Q0.49 slim Cargo lock reconstruction retired

on:
  push:
    branches:
      - diagnostic/hepta-intelligence-q0-49-slim-lock-20260830

permissions:
  contents: read

concurrency:
  group: hepta-intelligence-q0-49-slim-lock
  cancel-in-progress: true

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-slim
    steps:
      - run: echo "superseded by dependency publisher"
