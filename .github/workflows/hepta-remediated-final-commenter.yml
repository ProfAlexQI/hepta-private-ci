name: Hepta remediated final CI commenter

on:
  push:
    branches:
      - codex/hepta-remediated-observer-final-20260902
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: hepta-remediated-final-commenter-20260902
  cancel-in-progress: true

jobs:
  comment:
    runs-on: ubuntu-24.04
    timeout-minutes: 110
    env:
      HEAD_BRANCH: codex/hepta-v8-repository-closure-remediated-20260902
      REPORT_BRANCH: codex/hepta-remediated-final-report-20260902
    steps:
      - name: Wait for immutable observation and publish exact PR comment
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          pr_json="$(gh api "/repos/${REPOSITORY}/pulls?state=open&head=TrillionniumFoundation:${HEAD_BRANCH}")"
          test "$(jq 'length' <<<"$pr_json")" = "1"
          pr_number="$(jq -r '.[0].number' <<<"$pr_json")"
          expected_head="$(jq -r '.[0].head.sha' <<<"$pr_json")"

          deadline=$((SECONDS + 6300))
          worktree="$(mktemp -d)"
          while (( SECONDS < deadline )); do
            if git clone --depth=1 --branch "$REPORT_BRANCH" "https://x-access-token:${GH_TOKEN}@github.com/${REPOSITORY}.git" "$worktree/report" 2>/dev/null; then
              if [[ -f "$worktree/report/summary.json" ]]; then
                report_head="$(jq -r '.headSha' "$worktree/report/summary.json")"
                if [[ "$report_head" = "$expected_head" ]]; then
                  break
                fi
              fi
              rm -rf "$worktree/report"
            fi
            sleep 20
          done

          test -f "$worktree/report/summary.json"
          test "$(jq -r '.headSha' "$worktree/report/summary.json")" = "$expected_head"
          active="$(jq -r '.counts.active' "$worktree/report/summary.json")"
          failed="$(jq -r '.counts.failed' "$worktree/report/summary.json")"
          successful="$(jq -r '.counts.successful' "$worktree/report/summary.json")"
          total="$(jq -r '.counts.total' "$worktree/report/summary.json")"
          head_tree="$(jq -r '.headTree' "$worktree/report/summary.json")"

          {
            echo "<!-- hepta-remediated-final-observation -->"
            echo "## Immutable final-candidate CI observation"
            echo
            echo "- head SHA: \`${expected_head}\`"
            echo "- head tree: \`${head_tree}\`"
            echo "- workflows: ${total}"
            echo "- successful: ${successful}"
            echo "- active: ${active}"
            echo "- failed: ${failed}"
            echo
            if [[ "$active" = "0" && "$failed" = "0" ]]; then
              echo "All observed repository-owned pull-request workflows are green for this immutable head. External hardware, future-time, independent-safety, operator-acceptance and governed-release evidence gates remain fail-closed."
            else
              echo "Repository-owned closure is not yet green. Failed or active workflows:"
              jq -r '.workflows[] | select(.status != "completed" or (.conclusion != "success" and .conclusion != "skipped" and .conclusion != "neutral")) | "- `\(.name)`: status=`\(.status)`, conclusion=`\(.conclusion)`, run=`\(.id)`"' "$worktree/report/summary.json"
            fi
          } > "$worktree/comment.md"

          existing="$(gh api "/repos/${REPOSITORY}/issues/${pr_number}/comments?per_page=100" --jq '.[] | select(.body | contains("<!-- hepta-remediated-final-observation -->")) | .id' | head -n1)"
          if [[ -n "$existing" ]]; then
            gh api --method PATCH "/repos/${REPOSITORY}/issues/comments/${existing}" --raw-field body="$(cat "$worktree/comment.md")" >/dev/null
          else
            gh api --method POST "/repos/${REPOSITORY}/issues/${pr_number}/comments" --raw-field body="$(cat "$worktree/comment.md")" >/dev/null
          fi

          test "$active" = "0"
          test "$failed" = "0"
