name: Hepta Intelligence Q0.49 macOS publisher retired

on:
  push:
    branches:
      - automation/hepta-intelligence-q0-49-macos-publisher-20260830

permissions:
  contents: read

concurrency:
  group: hepta-intelligence-q0-49-macos-publisher
  cancel-in-progress: true

jobs:
  retired:
    if: ${{ false }}
    runs-on: macos-15
    steps:
      - run: echo "Q0.49 macOS publisher superseded by selected Q0.50 admission PR #232"
