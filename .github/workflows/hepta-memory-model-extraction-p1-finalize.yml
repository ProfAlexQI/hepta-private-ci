name: Hepta P1 architecture gap finalizer retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    name: Self-mutating lane is retired
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo "P1 materialization now runs in a read-only qualification lane."
          echo "Validated patches are applied explicitly through reviewed repository writes."
