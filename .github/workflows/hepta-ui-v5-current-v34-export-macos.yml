name: Hepta UI v5 current v34 export superseded

on:
  push:
    branches:
      - ops/ui-v5-current-v34-export-macos-20260830
    paths:
      - .github/workflows/hepta-ui-v5-current-v34-export-macos.yml

permissions:
  contents: read

concurrency:
  group: hepta-ui-v5-current-v34-export-macos
  cancel-in-progress: true

jobs:
  superseded:
    if: ${{ false }}
    runs-on: macos-14
    steps:
      - run: echo "superseded after verified objects were published through Git Data API"
