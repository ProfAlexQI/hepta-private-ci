name: archived-ui-v4-contract

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    name: Historical UI v4 contract archive
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Explain canonical replacement
        shell: bash
        run: |
          set -euo pipefail
          echo "This historical UI v4 workflow is retained for audit and manual replay only."
          echo "Repository-controlled executable qualification moved to hepta-ui-v4-final-gap-closure-v4."
          echo "Physical-device, operator, browser-human, and product-host evidence remain in their dedicated governed workflows."
