name: Hepta UI v5 current v23 x64 tombstone

on:
  push:
    branches:
      - ops/ui-v5-current-v23-x64-20260830
    paths:
      - .github/workflows/hepta-ui-v5-current-v23-x64.yml

permissions:
  contents: read

concurrency:
  group: hepta-ui-v5-current-v23-x64
  cancel-in-progress: true

jobs:
  superseded:
    if: ${{ false }}
    runs-on: ubuntu-slim
    steps:
      - run: echo superseded-by-current-v30
