name: Hepta UI v5 current v32 macOS xlarge superseded

on:
  push:
    branches:
      - ops/ui-v5-current-v32-macos-xlarge-20260830
    paths:
      - .github/workflows/hepta-ui-v5-current-v32-macos-xlarge.yml

permissions:
  contents: read

concurrency:
  group: hepta-ui-v5-current-v32-macos-xlarge
  cancel-in-progress: true

jobs:
  superseded:
    if: ${{ false }}
    runs-on: macos-15-xlarge
    steps:
      - run: echo "superseded after v31 acquired an assigned runner"
