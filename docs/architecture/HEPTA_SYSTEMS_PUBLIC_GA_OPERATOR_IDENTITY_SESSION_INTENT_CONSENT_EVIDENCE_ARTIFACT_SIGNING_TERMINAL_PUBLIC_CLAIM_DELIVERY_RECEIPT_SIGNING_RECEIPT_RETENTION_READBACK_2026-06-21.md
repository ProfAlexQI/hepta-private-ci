# Terminal Public Claim Delivery Receipt Artifact Signing Receipt Signing Receipt Retention/Expiry/GC Readback

This readback consumes the terminal public claim delivery receipt artifact signing receipt signing receipt retention/expiry/GC attachment and preserves the branch-specific retention denial state for final indexing.

Status: ready-but-blocked. It does not invoke signing retention gates, signing receipt audit gates, signing receipt target gates, terminal live gates, provider calls, credential reads, external delivery, deployment, release, or Public GA.

The readback keeps signing receipt retention policy, TTL lease, expiry timer, garbage-collection queue/decision/execution, archive, compaction, external/Telegram retention, approval, authority, install, restart, active-binary mutation, provider invocation, credential access, Public GA claim, and public release publication false.
