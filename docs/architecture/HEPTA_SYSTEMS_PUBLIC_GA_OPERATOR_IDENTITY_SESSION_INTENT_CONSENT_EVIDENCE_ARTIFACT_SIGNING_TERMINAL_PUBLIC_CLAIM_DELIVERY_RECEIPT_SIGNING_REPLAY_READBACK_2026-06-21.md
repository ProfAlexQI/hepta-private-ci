# Terminal Public Claim Delivery Receipt Artifact Signing Receipt Replay/Idempotency Readback

This readback consumes the terminal public claim delivery receipt artifact signing receipt replay/idempotency attachment and projects a static ready-but-blocked snapshot.

It does not invoke signing replay gates, signing receipt target gates, terminal live gates, public claim gates, provider calls, credential reads, or external delivery.

The readback keeps signing receipt replay, duplicate identity, idempotency key/state, nonce replay, cross-scope reuse, status upgrade, ack replay, hash/status rebind, receipt replay surfaces, external/Telegram replay, approval, authority, install, restart, active-binary mutation, provider invocation, credential access, Public GA claim, and public release publication false.
