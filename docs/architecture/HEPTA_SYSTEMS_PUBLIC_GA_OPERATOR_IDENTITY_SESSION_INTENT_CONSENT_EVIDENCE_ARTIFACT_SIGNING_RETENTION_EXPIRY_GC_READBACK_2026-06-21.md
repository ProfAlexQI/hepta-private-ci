# Public GA Operator Identity/Session Intent/Consent Evidence Artifact Signing Retention/Expiry/GC Readback

This readback consumes the local artifact signing retention/expiry/GC
attachment and projects a stable ready-but-blocked readback for the Public GA
operator identity/session intent/consent evidence chain.

The readback confirms that retention policy, TTL lease, expiry timestamps,
expiry schedulers, expiry timers, expiry acknowledgements, garbage-collection
queues, GC scans, GC decisions, archive, compaction, evidence retention,
operator approval, release-publication authority, activation authority,
install/restart paths, active-binary mutation, provider or model invocation,
credential/secret reads, Public GA, public release, terminal live gates, and
rollback execution remain blocked.

It does not invoke the artifact signing retention/expiry/GC gate, the artifact
signing audit/evidence gate, any Public GA operator approval packet, any
terminal live gate, any long soak, any live URL, or any external send.
