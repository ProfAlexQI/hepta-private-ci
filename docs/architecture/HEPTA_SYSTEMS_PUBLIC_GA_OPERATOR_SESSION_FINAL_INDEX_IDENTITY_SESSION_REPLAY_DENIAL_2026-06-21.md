# Public GA Operator Identity/Session Replay Denial Attachment

This attachment consumes the Public GA operator session non-binding final index
and source-probes the operator identity/session replay cross-binding denial gate
and note. It does not invoke replay gates, identity/session binding gates,
intent/consent gates, Public GA operator packet, live endpoints, or long soak.

The attachment is ready-but-blocked. It keeps identity hash replay, session token
replay, cross-session binding, replay acceptance, replay-derived authority,
public release, and Public GA promotion false.

The attachment carries the canonical terminal closure backfeed from the source
session final index: 17 release/live blockers across 4 ready categories, with
runner selector blockers=2 and dirty worktree owner-freeze blockers=2. This is
read-model context only; the local replay denial blocker count remains 26.
