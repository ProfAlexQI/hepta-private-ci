# Upstream intake archive

This directory holds human-readable intake and semantic-absorption snapshots
that have been superseded by a newer reviewed intake.

Machine-verified JSON manifests remain at their original
`docs/architecture/` paths because their paths and hashes are part of the
active intake and semantic-absorption trust boundary.

The current human-readable intake and absorption record also remain in
`docs/architecture/`.
