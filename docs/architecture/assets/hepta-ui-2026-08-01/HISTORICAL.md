# Historical evidence only

This bundle captures the earlier `ui/native-productization` selective-module
architecture. It is superseded by the upstream-first Native line and is not an
active readiness input. The JSON files are intentionally preserved rather than
rewritten to look current.

Use `scripts/hepta-ui-current-readiness.sh` to generate a new, HEAD-bound
evidence directory.
