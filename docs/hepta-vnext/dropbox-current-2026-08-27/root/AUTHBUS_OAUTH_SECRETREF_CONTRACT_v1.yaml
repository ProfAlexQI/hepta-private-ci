schema: hepta.authbus.contract-projection.v1
plan_id: AUTHBUS-PLAN-2026-08-26
revision: "1.3"
status: PLANNING_ONLY_IMPLEMENTATION_BACKLOG
normative: false
execution_input: false
projection_only: true
generated_from_registry: true
source_registry_ref: OpenClaw/AUTHBUS_CANONICAL_CONTRACT_REGISTRY_v1.yaml#/registry/contracts/oauth_secretref
source_registry_status: REQUIRED_AT_B2
source_registry_digest: PENDING_AT_B2
projection_transform: {allowed: [metadata_wrapper, operation_map], forbidden: [semantic_rename, field_drop, status_reinterpretation]}
contract:
  id: hepta.auth.opaque-secret-operation.v1
  decode_only_aliases: [hepta.auth.secretref-operation.v1]
  owner: authbusd
  writer: authbusd
  adapter_boundary: process_bound_secretref_only
  raw_secret_bytes: forbidden
  secret_ref_shape: {required: [backend_id, mount, key_id, version, secret_digest], forbidden: [secret_bytes, raw_secret, access_token, refresh_token, client_secret, authorization_header, provider_headers]}
  request_required: [schema_version, operation_id, refresh_operation_key, command_id, run_id, profile_id, provider_id, token_family_id, secret_ref, expected_secret_revision, idempotency_key, payload_digest, policy_digest, scope_digest, authority_epoch, owner_epoch, generation, fencing_token, logical_clock, causal_parent_event_id, deadline_at, purpose_digest, audience]
  claim_key: [provider_id, profile_id, token_family_id]
  token_family_writer: authbusd
  invalid_grant_policy: one_quarantine_then_reauthorize
  response_lost_policy: lookup_only_no_blind_retry
  operations:
    RefreshWithSecretRef:
      request_required: [schema_version, operation_id, refresh_operation_key, command_id, run_id, profile_id, provider_id, token_family_id, secret_ref, expected_secret_revision, idempotency_key, payload_digest, policy_digest, scope_digest, authority_epoch, owner_epoch, generation, fencing_token, logical_clock, causal_parent_event_id, deadline_at, purpose_digest, audience]
      response_required: [schema_version, response_id, operation_id, provider_id, profile_id, token_family_id, outcome, access_secret_ref, refresh_secret_ref, secret_revision, refresh_operation_key, provider_status, response_digest, idempotency_key, payload_digest, expected_secret_revision, authority_epoch, owner_epoch, generation, fencing_token]
      outcomes: [SUCCEEDED, TRANSIENT_FAILURE, INDETERMINATE, QUARANTINED]
      error_class: [NONE, INVALID_GRANT, UNAUTHORIZED, CONFLICT, TIMEOUT, UNAVAILABLE, SEALED, STALE_FENCE, SCHEMA_INVALID]
      success_requires: [access_secret_ref, refresh_secret_ref, secret_revision, response_digest]
      non_success_forbids: [access_secret_ref, refresh_secret_ref]
    RotateSecretRef:
      request_required: [schema_version, operation_id, refresh_operation_key, command_id, run_id, profile_id, provider_id, token_family_id, secret_ref, expected_secret_revision, idempotency_key, payload_digest, policy_digest, scope_digest, authority_epoch, owner_epoch, generation, fencing_token, logical_clock, causal_parent_event_id, deadline_at, purpose_digest, audience]
      response_required: [schema_version, response_id, operation_id, provider_id, profile_id, token_family_id, outcome, new_refresh_secret_ref, secret_revision, refresh_operation_key, response_digest, idempotency_key, payload_digest, expected_secret_revision, authority_epoch, owner_epoch, generation, fencing_token]
      outcomes: [SUCCEEDED, TRANSIENT_FAILURE, INDETERMINATE, QUARANTINED]
      error_class: [NONE, INVALID_GRANT, UNAUTHORIZED, CONFLICT, TIMEOUT, UNAVAILABLE, SEALED, STALE_FENCE, SCHEMA_INVALID]
      success_requires: [new_refresh_secret_ref, secret_revision, response_digest]
      non_success_forbids: [new_refresh_secret_ref]
    RefreshStatusByOperationKey:
      lookup_only: true
      dispatch: forbidden
      operation_name: RefreshStatusByOperationKey
      operation_aliases_decode_only: {status_lookup: StatusByEffectKey}
      request_required: [schema_version, operation_id, provider_id, profile_id, token_family_id, refresh_operation_key, idempotency_key, payload_digest, expected_secret_revision, authority_epoch, owner_epoch, generation, fencing_token, deadline_at, audience, expected_execution_mode, policy_digest]
      response_required: [schema_version, response_id, operation_id, provider_id, profile_id, token_family_id, refresh_operation_key, idempotency_key, payload_digest, expected_secret_revision, authority_epoch, owner_epoch, generation, fencing_token, outcome, secret_revision, response_digest, provider_status, status_revision, observed_at, binding_digest, evidence_profile, provider_query_receipt_digest, execution_mode, mode_attestation_digest, policy_digest, audience, key_epoch, issuer]
      response_nullable: [new_access_secret_ref, new_refresh_secret_ref, signature, key_id, issued_at, expires_at]
      outcomes: [SUCCEEDED, TRANSIENT_FAILURE, INDETERMINATE, QUARANTINED]
      error_class: [NONE, INVALID_GRANT, UNAUTHORIZED, CONFLICT, TIMEOUT, UNAVAILABLE, SEALED, STALE_FENCE, SCHEMA_INVALID]
  states: [IDLE, CLAIMED, IN_FLIGHT, SUCCEEDED, TRANSIENT_FAILURE, INDETERMINATE, RECONCILING, BACKOFF, QUARANTINED, MANUAL_REQUIRED]
  transitions: [[IDLE, claim, CLAIMED], [CLAIMED, dispatch, IN_FLIGHT], [IN_FLIGHT, rotated, SUCCEEDED], [IN_FLIGHT, invalid_grant, QUARANTINED], [IN_FLIGHT, transient_failure, TRANSIENT_FAILURE], [TRANSIENT_FAILURE, retry_scheduled, BACKOFF], [TRANSIENT_FAILURE, retry_budget_exhausted, MANUAL_REQUIRED], [IN_FLIGHT, response_unknown, INDETERMINATE], [INDETERMINATE, lookup, RECONCILING], [RECONCILING, lookup_rotated, SUCCEEDED], [RECONCILING, lookup_invalid_grant, QUARANTINED], [RECONCILING, lookup_transient_failure, BACKOFF], [RECONCILING, lookup_retryable, BACKOFF], [RECONCILING, manual_required, MANUAL_REQUIRED], [MANUAL_REQUIRED, manual_evidence_submitted, RECONCILING], [BACKOFF, claim_again, CLAIMED]]
  decode_only_event_aliases: {verified_rotation: rotated, response_lost: response_unknown, unavailable: manual_required, verified_evidence: manual_evidence_submitted, budget_remaining: retry_scheduled, retryable: lookup_retryable}
  status_lookup_classification_aliases_decode_only: {ROTATED: SUCCEEDED, INVALID_GRANT: QUARANTINED, TRANSIENT_FAILURE: BACKOFF, UNKNOWN: INDETERMINATE}
  invariants: [operation_key_durable_before_call, one_token_family_singleflight, invalid_grant_quarantine_once, no_blind_retry_after_response_loss, old_fence_callback_rejected, no_raw_token_projection]
  terminal: [SUCCEEDED, QUARANTINED]
  compatibility: {deadline: decode_only, deadline_at: encode_only, invalid_grant: classification_only_alias, rotated: result_kind_alias, manual_required: nonterminal_hold_alias, raw_token_fields: reject}
  fixture_manifest: {manifest: authbus_contract_vectors_v1/opaque_secret_operation, positive: [refresh_success, rotate_success, invalid_grant_quarantine, transient_backoff, response_lost_indeterminate, status_lookup], negative: [raw_field, changed_digest, stale_fence, blind_retry, synthetic_external_signature]}
