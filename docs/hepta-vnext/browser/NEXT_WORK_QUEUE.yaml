{
  "schema": "hepta.browser.next_work_queue.v1",
  "schema_version": 1,
  "lane": "WEB_C1_SERVO_BOOTSTRAP",
  "authority": "qualification_only",
  "ordering_rule": "complete exact source provenance and at least one platform-private transport before importing or executing Servo",
  "tasks": [
    {
      "id": "C1-001",
      "title": "Freeze Servo source import topology",
      "status": "COMPLETE_CONTRACT_FROZEN",
      "depends_on": [],
      "inputs": [
        "docs/hepta-vnext/browser/SERVO_UPSTREAM_PIN.json",
        "docs/hepta-vnext/browser/ADR-0001-servo-private-transport.md"
      ],
      "outputs": [
        "docs/hepta-vnext/browser/SERVO_SOURCE_IMPORT_TOPOLOGY.yaml",
        "third_party/servo-patches/PATCH_INVENTORY.json",
        "third_party/servo-patches/README.md"
      ],
      "completed": [
        "exact Servo commit, tree, version, edition, MSRV and license frozen",
        "reviewed upstream file and blob inventory frozen",
        "Servo kept outside the codex-rs Cargo workspace",
        "worker artifact boundary selected",
        "initial allowed and forbidden Servo surfaces frozen",
        "governed patch queue created and remains empty"
      ],
      "acceptance": [
        "all future imported source binds the exact Servo commit/tree",
        "no unpinned branch or git dependency is allowed",
        "MPL-2.0 notices and source obligations are recorded",
        "changing the topology requires a successor decision"
      ]
    },
    {
      "id": "C1-002",
      "title": "Create Servo provenance and SBOM generator",
      "status": "PARTIAL_SOURCE_RECEIPT_GENERATOR_IMPLEMENTED",
      "depends_on": ["C1-001"],
      "implemented_outputs": [
        "scripts/generate-hepta-servo-provenance.py",
        "scripts/test_generate_hepta_servo_provenance.py",
        "docs/hepta-vnext/browser/hepta.servo.source_receipt.v1.schema.json",
        "docs/hepta-vnext/browser/SERVO_PROVENANCE.md"
      ],
      "completed": [
        "offline exact HEAD/tree verification",
        "dirty, staged and untracked source rejection",
        "reviewed Git blob verification",
        "required source file SHA-256 inventory",
        "MPL-2.0 text check",
        "patch inventory and patch digest binding",
        "canonical path-free atomic source receipt output",
        "negative authority fields fixed false",
        "dependency-free primitive unit tests"
      ],
      "remaining": [
        "run the generator against the exact clean Servo checkout",
        "validate the generated receipt independently",
        "Cargo package and feature SBOM",
        "native/system dependency inventory",
        "complete license inventory and notices",
        "toolchain compatibility receipt",
        "offline source bundle and SHA256SUMS"
      ],
      "acceptance": [
        "generator uses only exact clean source",
        "second run produces byte-identical canonical output",
        "reviewed upstream blobs match the frozen topology",
        "unknown, dirty, missing or symlinked source fails closed",
        "receipt contains no machine-local absolute paths",
        "SBOM and toolchain records bind the same source tree"
      ]
    },
    {
      "id": "C1-003",
      "title": "Implement private worker protocol and process launcher",
      "status": "PARTIAL_UNIX_AND_PORTABLE_QUALIFICATION_SCAFFOLDS_IMPLEMENTED",
      "depends_on": ["C1-001"],
      "outputs": [
        "codex-rs/hepta-shadow-qualification/src/browser_worker_protocol.rs",
        "codex-rs/hepta-shadow-qualification/src/browser_worker_harness.rs",
        "codex-rs/hepta-shadow-qualification/src/browser_worker_unix.rs",
        "codex-rs/hepta-shadow-qualification/src/bin/hepta-browser-worker-qualification.rs",
        "codex-rs/hepta-shadow-qualification/src/browser_worker_tests.rs",
        "codex-rs/hepta-shadow-qualification/tests/browser_worker_process.rs",
        "codex-rs/hepta-shadow-qualification/tests/browser_worker_unix_socketpair.rs",
        "docs/hepta-vnext/browser/WORKER_PROTOCOL.md"
      ],
      "completed": [
        "versioned canonical JSON protocol with 65536-byte maximum frame",
        "one-use 32-byte startup capability and domain-separated digest",
        "session, generation, transport and sequence validation",
        "closed-authority verification",
        "qualification-only private stdin/stdout child process harness",
        "pathless inherited Unix socketpair qualification harness",
        "spawned PID and WorkerReady PID binding",
        "existing non-symlink worker executable validation",
        "parent EOF, bounded timeout, kill-on-drop and explicit shutdown behavior",
        "fixture BrowserActor request/response round trips"
      ],
      "remaining": [
        "independent compile, unit, process and Clippy qualification",
        "Unix peer credential and executable artifact digest evidence",
        "Unix parent-death/process-group enforcement",
        "Windows SID-restricted named pipe",
        "Windows Job Object enforcement",
        "real Servo event-loop adapter"
      ],
      "acceptance": [
        "no TCP, WebSocket or filesystem listener",
        "wrong capability/session/generation/sequence closes channel",
        "oversized, noncanonical and unknown-field frames fail closed",
        "child cannot reconnect to a closed generation",
        "reported child PID matches the process spawned by the parent",
        "qualification transports are never described as production-qualified"
      ]
    },
    {
      "id": "C1-004",
      "title": "Build minimal pinned Servo worker adapter",
      "status": "BLOCKED_BY_SOURCE_RECEIPT_AND_QUALIFIED_PLATFORM_TRANSPORT",
      "depends_on": ["C1-002", "C1-003"],
      "outputs": [
        "create and close one session",
        "local-fixture navigation",
        "bounded semantic observation",
        "bounded click, type, clear and wait",
        "status, cancel and teardown"
      ],
      "forbidden": [
        "raw WebDriver passthrough",
        "arbitrary JavaScript execution",
        "Servo preference mutation",
        "raw cookie, storage or profile export",
        "external network",
        "multiple WebViews or windows"
      ],
      "acceptance": [
        "local fixture renders in one real Servo WebView",
        "typed observation is produced",
        "one bounded action changes page revision",
        "worker teardown removes the ephemeral profile",
        "no wildcard, loopback TCP or WebSocket control listener exists"
      ]
    },
    {
      "id": "C1-005",
      "title": "Bind real Servo worker to Browser Actor",
      "status": "BLOCKED_BY_C1-004",
      "depends_on": ["C1-004"],
      "outputs": [
        "real BrowserEngine implementation",
        "fixture Actor and real engine parity corpus",
        "human takeover race tests",
        "crash and cancellation outcomes"
      ],
      "acceptance": [
        "session, generation, epoch and revision fences survive transport",
        "stale semantic refs are rejected after real navigation",
        "same request and digest is idempotent",
        "indeterminate mutation is not blindly replayed"
      ]
    },
    {
      "id": "C1-006",
      "title": "Qualify sandbox, listener and egress posture",
      "status": "BLOCKED_BY_C1-004",
      "depends_on": ["C1-004"],
      "outputs": [
        "socket and listener inventory",
        "external egress denial proof",
        "private filesystem and profile proof",
        "resource limits and kill behavior"
      ],
      "acceptance": [
        "wildcard and loopback TCP listener count is zero",
        "external DNS, TCP, UDP and WebSocket attempts fail",
        "profile root rejects symlink and hardlink escape",
        "OOM or timeout terminates only the worker session"
      ]
    },
    {
      "id": "C1-007",
      "title": "Run C1 platform matrix",
      "status": "BLOCKED_BY_C1-005_AND_C1-006",
      "depends_on": ["C1-005", "C1-006"],
      "outputs": [
        "Linux x86_64 receipt",
        "macOS arm64 receipt",
        "Windows x86_64 receipt",
        "known-gap inventory"
      ],
      "acceptance": [
        "locked build and test commands pass",
        "artifact, source and toolchain digests are recorded",
        "all authority flags remain false",
        "results bind one Hepta and Servo source pair"
      ]
    },
    {
      "id": "C1-008",
      "title": "Seal WEB-C1 qualification receipt",
      "status": "BLOCKED_BY_C1-007",
      "depends_on": ["C1-007"],
      "outputs": ["hepta_browser_servo_runtime_qualification_v1 receipt"],
      "acceptance": [
        "receipt validates against the repository schema",
        "C1 status changes only through a separately reviewed current-pointer update",
        "no production caller, operator acceptance or promotion is created"
      ]
    }
  ],
  "environment_blocker": {
    "status": "GITHUB_ACTIONS_RUNNER_PREFLIGHT_FAILS_BEFORE_STEPS",
    "evidence": "blocking-ci run 33069454542 recorded Runner preflight failure with no step records; all dependent qualification jobs were skipped",
    "interpretation": "repository runner, billing or policy layer failure; not a code test result",
    "required_action": "repair GitHub Actions runner, billing, repository policy or action configuration, then rerun the exact Draft PR head before recording any PASS"
  }
}
