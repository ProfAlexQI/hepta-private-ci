# Hepta paper source locks v2

This orphan evidence line preserves exact copyright-compliant publisher abstract snapshots together with the pinned PMLR summary and arXiv v1 PDF/source bytes used by Hepta adaptive documentation. `MANIFEST.v2.json` binds every path, Git blob, byte length, SHA-256 and claim sentence index.

The product branch must pin one exact evidence commit and replay every digest from these bytes. Changing a product registry and its verifier together cannot substitute for this independent evidence object. These files grant no runtime, model, tool, network, acceptance, promotion or release authority.
